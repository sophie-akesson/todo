<!DOCTYPE html>
<html lang="sv">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To do</title>
  </head>
  <body>
    <div id="container">
      <header>
        <h1>To do</h1>
      </header>
      <main>
        <p>Sort:<a href="/edit?id=<%= taskId %>&page=<%= currentPage %>&sort=asc">Asc</a> <a href="/edit?id=<%= taskId %>&page=<%= currentPage %>&sort=desc">Desc</a></p>
        <ul>
          <% if (tasks.length > 0) { %>
            <% tasks.forEach((task)=> { %>
              <% const date = task.dueDate; %>
              <% const year = date.getFullYear(); %>
              <% let month = date.getMonth() + 1; %>
              <% if (month < 10) { month = '0' + month; } %>
              <% let day = date.getDate(); %>
              <% if (day < 10) { day = '0' + day; } %>
              <% let convertedDate = `${year}-${month}-${day}`; %>
              <li>
                <% if (taskId != task.id) { %>
                  <form action="/delete?id=<%= task.id %>&page=<%= currentPage %>&sort=<%= currentSort %>" method="post">
                    <input type="checkbox" name="checkbox" onChange="this.form.submit()">
                  </form>
                <div><p>Name: <%= task.name %></p></div>
                <div><p>Due date: <%= convertedDate %></p></div>
                <% } %>
                <% if (taskId == task.id) { %>
                  <form action="/edit?id=<%= task.id %>&page=<%= currentPage %>&sort=<%= currentSort %>" method="post">
                    <label for="editName">Name: </label>
                    <input
                      type="text"
                      name="editName"
                      id="editName"
                      value="<%= task.name %>"
                    />
                    <label for="editDate">Due date: </label>
                    <input
                      type="date"
                      name="editDate"
                      value="<%= convertedDate %>"
                    />
                    <button type="submit">Save</button>
                  </form>
                  <a href="/?page=<%= currentPage %>&sort=<%= currentSort %>">Cancel</a>
                <% } %>
                <% if (taskId != task.id) { %>
                  <div class="task-links">
                    <a href="/edit?id=<%= task.id %>&page=<%= currentPage %>&sort=<%= currentSort %>">Edit</a>
                <% } %>
                <a href="/delete?id=<%= task.id %>&page=<%= currentPage %>&sort=<%= currentSort %>">Delete</a>
              </div>
            </li>
            <% });%>
            <% } %>
            <% if (currentPage > totalPages) { %>
            <li>You have no tasks on this page. Please go to previous page.</li>
            <% } %>
            <% if (totalDocuments == 0) { %>
              <li>You have no tasks.</li>
            <% } %>
        </ul>
        <% if (currentPage > 1) { %>
          <a href="/?page=<%= currentPage - 1 %>&sort=<%= currentSort %>"><< Previous page</a>
          <% } %>
          <% if (currentPage < totalPages) { %>
          <a href="/?page=<%= currentPage + 1 %>&sort=<%= currentSort %>">Next page >></a>
          <% } %>  
      </main>
      <footer><%- include ("addtask"); -%></footer>
    </div>
  </body>
</html>
